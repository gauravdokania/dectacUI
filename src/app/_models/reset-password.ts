export class Reset {
    successMessage!: {
        email?: string;
        newpassword?: string;
        oldpassword?: string;
    }[]; // This defines an array of objects

    errorinfodvocollection!: {
        error?: string;
    }[]; // This defines an array of objects

}



